<!-- Placeholder: the editor-workspace work package replaces this file with
     the Edit view (canvas stage, tool rail, tool options, bottom bar). It
     reads everything from getSession() and takes no props. -->
<div class="workspace" data-testid="workspace"></div>

<style>
  .workspace {
    flex: 1;
    min-height: 0;
  }
</style>
